import Link from"next/link";import{AppShell}from"../../components/app-shell";import{requireServerSession}from"../../lib/auth/server-session";
export default async function SettingsPage(){await requireServerSession("/settings");return <AppShell><section className="capabilityPage"><header><p className="eyebrow">Safety and access</p><h1>Settings</h1><p>Agent policy changes remain locked until you deliberately enter edit mode.</p></header><div className="capabilityFacts"><div><span>01</span><p>Execution networks and spending controls are managed in Agent.</p></div><div><span>02</span><p>Disconnecting the wallet removes access to authenticated product controls.</p></div><div><span>03</span><p>Emergency and autonomy settings are enforced server-side.</p></div></div><Link className="capabilityCommand" href="/agent">Open Agent controls</Link></section></AppShell>}
