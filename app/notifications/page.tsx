import { AppShell } from "../../components/app-shell";import{CapabilityPage}from"../../components/product/capability-page";import{requireServerSession}from"../../lib/auth/server-session";
export default async function NotificationsPage(){await requireServerSession("/notifications");return <AppShell><CapabilityPage eyebrow="Delivery evidence" title="Notifications" symbol="notify" description="Execution confirmations and failures can be delivered to connected channels without changing transaction outcomes." facts={["Delivery attempts are recorded independently from execution status.","Telegram connections are scoped to the authenticated agent.","A notification failure never hides a confirmed or failed receipt."]}/></AppShell>}
