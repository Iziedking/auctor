import { AppShell } from "../../components/app-shell";import{CapabilityPage}from"../../components/product/capability-page";import{requireServerSession}from"../../lib/auth/server-session";
export default async function PortfolioPage(){await requireServerSession("/portfolio");return <AppShell><CapabilityPage eyebrow="Capital view" title="Portfolio" symbol="portfolio" description="Ask Auctor for wallet balances, exposure, and position explanations. Read data stays separate from permission to move funds." facts={["Balances are scoped to the connected agent wallet.","Every proposed adjustment still passes policy and simulation.","Confirmed movements are linked to KeeperHub receipts in Audit trail."]}/></AppShell>}
