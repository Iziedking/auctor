import { AppShell } from "../../components/app-shell";import{CapabilityPage}from"../../components/product/capability-page";import{requireServerSession}from"../../lib/auth/server-session";
export default async function StrategiesPage(){await requireServerSession("/strategies");return <AppShell><CapabilityPage eyebrow="Bounded intent" title="Strategies" symbol="strategy" description="Describe the outcome, risk boundary, assets, and cadence. Auctor turns it into deterministic guarded actions." facts={["Language models interpret intent but cannot authorize transactions.","KeeperHub simulation is required before every write.","Guarded and autonomous modes remain constrained by saved controls."]}/></AppShell>}
