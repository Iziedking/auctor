import { AppShell } from "../../components/app-shell";import{CapabilityPage}from"../../components/product/capability-page";import{requireServerSession}from"../../lib/auth/server-session";
export default async function MemoryPage(){await requireServerSession("/memory");return <AppShell><CapabilityPage eyebrow="AgentQA memory" title="Memory" symbol="memory" description="Auctor recalls encrypted preferences and prior decisions before interpreting a new instruction." facts={["Memory identity remains stable for the same authenticated account.","Recalled evidence is attached to affected decisions.","Memory never overrides policy, approval, or spending limits."]}/></AppShell>}
