import { AppShell } from "../../components/app-shell";import{CapabilityPage}from"../../components/product/capability-page";import{requireServerSession}from"../../lib/auth/server-session";
export default async function TasksPage(){await requireServerSession("/tasks");return <AppShell><CapabilityPage eyebrow="Autonomous work" title="Tasks" symbol="tasks" description="Create watches, recurring checks, and bounded execution instructions through Command." facts={["Schedules inherit the agent's daily and single-action limits.","A task cannot bypass emergency stop or allowed-chain policy.","Runs and failures are preserved in the agent audit trail."]}/></AppShell>}
