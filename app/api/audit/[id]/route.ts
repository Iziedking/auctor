import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hashToken}from"../../../../lib/auth/email.ts";import{createDatabaseAuditSource}from"../../../../lib/audit/database-source.ts";import{createFixtureAuditSource}from"../../../../lib/audit/source.ts";import{loadConfig,runtimeEnvironment}from"../../../../lib/config.ts";import{resolveSession}from"../../../../lib/db/auth-repository.ts";import{createDatabase}from"../../../../lib/db/client.ts";
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const id=(await params).id;if(process.env.AUCTOR_E2E_AUTH==="1"){const result=await createFixtureAuditSource().get(id);if(result.kind==="not_found")return NextResponse.json({error:"not_found"},{status:404});return NextResponse.json({item:result.kind==="ready"?result.value:null})}const token=(await cookies()).get("auctor_session")?.value;if(!token)return NextResponse.json({error:"unauthorized"},{status:401});const config=loadConfig(runtimeEnvironment());if(!config.database.url)return NextResponse.json({error:"audit_unavailable"},{status:503});const database=createDatabase(config.database.url);try{const session=await resolveSession(database.db,hashToken(token));if(!session)return NextResponse.json({error:"unauthorized"},{status:401});const result=await createDatabaseAuditSource(database.db,session.agentId,session.agentName).get(id);if(result.kind==="not_found")return NextResponse.json({error:"not_found"},{status:404});return NextResponse.json({item:result.kind==="ready"?result.value:null})}finally{await database.close()}}
