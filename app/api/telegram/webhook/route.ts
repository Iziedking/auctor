import{and,eq}from"drizzle-orm";import{NextResponse}from"next/server";import{createChannelPairingService}from"../../../../lib/auth/channel-service.ts";import{deriveMemoryIdentity}from"../../../../lib/auth/memory-identity.ts";import{executeApprovedPreview}from"../../../../lib/chat/approved-trade.ts";import{createPreviewTradeMarketData}from"../../../../lib/chat/preview-market-data.ts";import{createRuntimeChatPipeline}from"../../../../lib/chat/runtime-pipeline.ts";import{loadConfig,runtimeEnvironment}from"../../../../lib/config.ts";import{createDrizzleAgentPolicyRepository}from"../../../../lib/db/agent-policy-repository.ts";import{createDrizzleChannelPairingRepository}from"../../../../lib/db/channel-pairing-repository.ts";import{createDatabase}from"../../../../lib/db/client.ts";import{channelConnections,memoryIdentities,agents}from"../../../../lib/db/schema.ts";import{createDrizzleExecutionRepository}from"../../../../lib/db/execution-repository.ts";import{createExecutionService}from"../../../../lib/execution/service.ts";import{createKeeperHubClient}from"../../../../lib/keeperhub/client.ts";import{createMemoryClient}from"../../../../lib/memory/client.ts";import{createTelegramNotifier}from"../../../../lib/notifications/telegram.ts";import{handleConnectCommand,parseTelegramUpdate,telegramChatText}from"../../../../lib/telegram/update.ts";
export async function POST(request:Request){const config=loadConfig(runtimeEnvironment());if(!config.notifications.telegramToken||!config.database.url)return NextResponse.json({ok:false},{status:503});const secret=process.env.TELEGRAM_WEBHOOK_SECRET;if(secret&&request.headers.get("x-telegram-bot-api-secret-token")!==secret)return NextResponse.json({ok:false},{status:401});let body:unknown;try{body=await request.json()}catch{return NextResponse.json({ok:true})}const update=parseTelegramUpdate(body);if(!update?.message?.text)return NextResponse.json({ok:true});const chatId=String(update.message.chat.id);const text=update.message.text.trim();const database=createDatabase(config.database.url);const telegram=createTelegramNotifier({token:config.notifications.telegramToken,mockMode:false});try{const pairing=createChannelPairingService({repository:createDrizzleChannelPairingRepository(database.db)});const connectReply=await handleConnectCommand({text,chatId,pairing});if(connectReply){await telegram.send({chatId,text:connectReply});return NextResponse.json({ok:true})}const rows=await database.db.select({userId:channelConnections.userId,agentId:channelConnections.agentId,name:agents.name,autonomyMode:agents.autonomyMode,walletAddress:agents.khWalletAddress,memoryKey:memoryIdentities.memoryKey}).from(channelConnections).innerJoin(agents,eq(agents.id,channelConnections.agentId)).innerJoin(memoryIdentities,eq(memoryIdentities.userId,channelConnections.userId)).where(and(eq(channelConnections.provider,"telegram"),eq(channelConnections.externalIdentity,chatId))).limit(1);const account=rows[0];if(!account){await telegram.send({chatId,text:"Connect this chat first from Notifications on auctor.space, then send /connect CODE."});return NextResponse.json({ok:true})}if(!account.walletAddress){await telegram.send({chatId,text:"Your KeeperHub agent wallet is not provisioned yet. Sign in at auctor.space and open Agent."});return NextResponse.json({ok:true})}const pipeline=createRuntimeChatPipeline({...config,keeperhub:{...config.keeperhub,walletAddress:account.walletAddress}});const identity=deriveMemoryIdentity({userId:account.userId,memoryKey:account.memoryKey,masterPassphrase:config.agent.memoryPassphrase??"",folder:`agent-${account.agentId}`});const memory=createMemoryClient({baseUrl:config.memory.url});const recalled=await memory.recall({user:identity.user,passphrase:identity.passphrase,folder:identity.folder,query:text});const recalledMemory=recalled.kind==="recalled"?recalled.records:[];const approved=/^\/approve\s+/i.test(text);const command=approved?text.replace(/^\/approve\s+/i,""):text;const preview=await pipeline.handle({text:command,correlationId:`telegram-${update.message.message_id}-${chatId}`,recalledMemory});if(!approved){await telegram.send({chatId,text:telegramChatText(preview)});return NextResponse.json({ok:true})}if(preview.kind!=="preview"){await telegram.send({chatId,text:telegramChatText(preview)});return NextResponse.json({ok:true})}if(!config.keeperhub.apiKey){await telegram.send({chatId,text:"Live KeeperHub execution is not configured."});return NextResponse.json({ok:true})}const keeperHub=createKeeperHubClient({baseUrl:config.keeperhub.baseUrl,apiKey:config.keeperhub.apiKey});const result=await executeApprovedPreview({agentId:account.agentId,preview,recalledMemory,policyRepository:createDrizzleAgentPolicyRepository(database.db),marketData:createPreviewTradeMarketData(preview),execution:createExecutionService({repository:createDrizzleExecutionRepository(database.db),keeperHub})});if(result.kind==="executed"){await telegram.send({chatId,text:`Auctor execution ${result.audit.status}.\nAudit: ${result.audit.id}\nTransaction: ${result.audit.transactionHash??"pending receipt"}`})}else await telegram.send({chatId,text:`Execution stopped safely: ${"error"in result?result.error.message:result.reason}`});return NextResponse.json({ok:true})}catch{await telegram.send({chatId,text:"Auctor could not process that message. No transaction was submitted."});return NextResponse.json({ok:true})}finally{await database.close()}}
