import assert from"node:assert/strict";import{test}from"node:test";import{formatMemoryEvent,temporalRecallQuery,validTimeZone}from"../lib/memory/events.ts";
test("memory events contain canonical and user-local timestamps plus searchable metadata",()=>{const text=formatMemoryEvent({type:"preference",source:"web",content:"Use Base Sepolia only.",importance:"high",correlationId:"chat-1",occurredAt:new Date("2026-08-13T01:30:00Z"),timeZone:"America/New_York"});assert.match(text,/occurred_at_utc: 2026-08-13T01:30:00.000Z/);assert.match(text,/occurred_at_local: 2026-08-12 21:30:00 GMT-4/);assert.match(text,/time_zone: America\/New_York/);assert.match(text,/type: preference/);assert.match(text,/correlation_id: chat-1/)});
test("recall query resolves relative dates in the requested timezone",()=>{const query=temporalRecallQuery("What did I decide yesterday?",new Date("2026-08-13T12:00:00Z"),"Asia/Tokyo");assert.match(query,/Current canonical time: 2026-08-13T12:00:00.000Z/);assert.match(query,/Current user time zone: Asia\/Tokyo/);assert.match(query,/What did I decide yesterday\?/)});
test("invalid or missing timezones safely fall back to UTC",()=>{assert.equal(validTimeZone("Not\/AZone"),"UTC");assert.equal(validTimeZone(undefined),"UTC");assert.match(formatMemoryEvent({type:"observation",source:"system",content:"Fallback",timeZone:"bad"}),/time_zone: UTC/)});
