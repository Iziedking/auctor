import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import{test}from"node:test";const read=(path:string)=>readFile(new URL(`../${path}`,import.meta.url),"utf8");
test("public login enables wallet access and locks email as coming soon",async()=>{const card=await read("components/auth/login-card.tsx");const walletButton=await read("components/auth/wallet-access-button.tsx");assert.match(card,/WalletAccessButton/);assert.match(walletButton,/Continue with wallet/);assert.match(card,/Email sign-in coming soon/);assert.match(card,/Coming soon/);assert.match(card,/input disabled/);assert.doesNotMatch(`${card}\n${walletButton}`,/SYSTEM \/ LIVE|AUDIT|MEMORY/)});
test("root renders the public gateway instead of redirecting to Audit",async()=>{const page=await read("app/page.tsx");assert.match(page,/PublicGateway/);assert.doesNotMatch(page,/redirect\("\/audit"\)/)});
