import assert from"node:assert/strict";import test from"node:test";import{isResearchRequest,runPaidResearch}from"../lib/research/orchestrator.ts";
test("research orchestration triggers only for explicit research requests",async()=>{assert.equal(isResearchRequest("research ETH market sentiment"),true);assert.equal(isResearchRequest("hello"),false);assert.equal(await runPaidResearch({text:"hello",async fetchResearch(){throw new Error("unused")}}),null)});
test("research orchestration exposes payment evidence",async()=>{const result=await runPaidResearch({text:"research ETH sentiment",async fetchResearch(){return{data:{sentiment:"bullish"},paid:true,priceUsd:.1,paymentTx:"0xpaid",refusedReason:null}}});assert.equal(result?.paymentTx,"0xpaid");assert.equal(result?.paid,true)});
