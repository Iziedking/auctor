"use client";
import{useEffect,useRef,useState}from"react";import{ConnectButton}from"@rainbow-me/rainbowkit";import{useAccount,useSignMessage}from"wagmi";
export interface ConnectedWallet{readonly walletAddress:`0x${string}`;readonly chainId:number;signMessage(message:string):Promise<string>}
export function WalletAccessButton({busy,onAuthenticate}:{busy:boolean;onAuthenticate(wallet:ConnectedWallet):Promise<void>}){const{address,chainId,isConnected}=useAccount();const{signMessageAsync}=useSignMessage();const[armed,setArmed]=useState(false);const running=useRef(false);useEffect(()=>{if(!armed||!isConnected||!address||!chainId||running.current)return;running.current=true;void onAuthenticate({walletAddress:address,chainId,signMessage:message=>signMessageAsync({message})}).finally(()=>{running.current=false;setArmed(false)})},[address,armed,chainId,isConnected,onAuthenticate,signMessageAsync]);return <ConnectButton.Custom>{({openConnectModal,mounted})=><button className="walletLogin" type="button" disabled={!mounted||busy} aria-busy={busy} onClick={()=>{setArmed(true);if(!isConnected)openConnectModal()}}>{busy?"Check your wallet…":"Continue with wallet"}</button>}</ConnectButton.Custom>}
