"use client";
import{useEffect,useState}from"react";import{useRouter,useSearchParams}from"next/navigation";import{connectWalletWithSiwe,type Eip1193Provider}from"../../lib/auth/wallet-client";import{readAuthJson}from"../../lib/auth/http-json";import{safeNextPath}from"../../lib/auth/redirect";import{BrandLoader}from"./brand-loader";import{LoginCard}from"./login-card";
const provider=()=> (window as typeof window&{ethereum?:Eip1193Provider}).ethereum;const minimumBrandDuration=()=>new Promise(resolve=>setTimeout(resolve,1800));
export function PublicGateway(){const router=useRouter();const params=useSearchParams();const next=safeNextPath(params.get("next"));const[checking,setChecking]=useState(true);const[busy,setBusy]=useState<"wallet"|"email"|null>(null);const[email,setEmail]=useState("");const[status,setStatus]=useState("");const[emailSent,setEmailSent]=useState(false);useEffect(()=>{let active=true;Promise.all([fetch("/backend/api/auth/session").catch(()=>null),minimumBrandDuration()]).then(([response])=>{if(!active)return;if(response?.ok){router.replace(next);router.refresh()}else setChecking(false)});return()=>{active=false}},[next,router]);async function wallet(){setBusy("wallet");setStatus("Connect your wallet, then approve the secure sign-in message.");try{await connectWalletWithSiwe({provider:provider()});router.replace(next);router.refresh()}catch(error){const rejected=error&&typeof error==="object"&&"code"in error&&error.code===4001;setStatus(rejected?"Wallet sign-in was cancelled. You can try again when ready.":error instanceof Error?error.message:"Wallet sign-in failed. Please try again.")}finally{setBusy(null)}}async function emailLogin(event:React.FormEvent){event.preventDefault();setBusy("email");setStatus("");try{const response=await fetch("/backend/api/auth/email/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,next})});const result=await readAuthJson(response);if(result&&typeof result==="object"&&"accepted"in result){setEmailSent(true);setStatus("The link expires in 15 minutes.")}}catch(error){setStatus(error instanceof Error?error.message:"Email sign-in is temporarily unavailable.")}finally{setBusy(null)}}if(checking)return <BrandLoader/>;return <LoginCard email={email} setEmail={setEmail} onWallet={()=>void wallet()} onEmail={emailLogin} busy={busy} status={status} emailSent={emailSent}/>}
