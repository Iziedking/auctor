import{AuctorLockup}from"../brand/auctor-mark";import{WalletAccessButton,type ConnectedWallet}from"./wallet-access-button";
export function LoginCard(props:{email:string;setEmail:(value:string)=>void;onWallet:(wallet:ConnectedWallet)=>Promise<void>;onEmail:(event:React.FormEvent)=>void;busy:"wallet"|"email"|null;status:string;emailSent:boolean}){return <main className="authGateway"><section className="loginCard" aria-labelledby="login-title"><header><AuctorLockup/><p className="eyebrow">Auctor access</p><h1 id="login-title">Your onchain agent starts here.</h1><p>Sign in once. Auctor keeps your context, follows your limits, and stays ready across every session.</p></header><WalletAccessButton busy={props.busy!==null} onAuthenticate={props.onWallet}/><p className="walletSafety">You will sign a secure message. This creates no transaction and costs no gas.</p><div className="authDivider"><span>or</span></div><section className="emailComingSoon" aria-label="Email sign-in coming soon"><div aria-hidden="true"><label>Email address</label><div><input disabled placeholder="you@example.com"/><button type="button" disabled>Email me a sign-in link</button></div></div><strong>Coming soon</strong><p>Email access is not enabled yet. Continue with a wallet to use Auctor.</p></section><p className="authStatus" aria-live="polite">{props.status}</p></section></main>}
