"use client";
import Link from"next/link";import{usePathname}from"next/navigation";import{AuctorSymbol}from"./brand/auctor-symbols";
const items=[{href:"/chat",label:"Command",symbol:"command"},{href:"/agent",label:"Agent",symbol:"agent"},{href:"/audit",label:"Audit trail",symbol:"audit"},{label:"Portfolio",symbol:"portfolio"},{label:"Tasks",symbol:"tasks"},{label:"Strategies",symbol:"strategy"},{label:"Memory",symbol:"memory"},{label:"Notifications",symbol:"notify"},{label:"Settings",symbol:"settings"}]as const;
export function RailNav(){const pathname=usePathname();return<nav>{items.map(item=>"href"in item?<Link key={item.label}href={item.href}className={"railLink"+(pathname.startsWith(item.href)?" active":"")}><AuctorSymbol name={item.symbol}/><span>{item.label}</span></Link>:<span key={item.label}className="railLink railLinkSoon"aria-disabled="true"><AuctorSymbol name={item.symbol}/><span>{item.label}</span><small>Soon</small></span>)}</nav>}
