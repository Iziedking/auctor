"use client";import{useEffect,useState}from"react";import Link from"next/link";import{BrainCircuit,Search,ShieldCheck,Radio,WalletCards,Zap}from"lucide-react";
type Agent={name:string;tradingStyle:string;riskProfile:string;autonomyMode:string;dailyCapUsd:string;perTradeCapUsd:string;allowedChains:string[];allowedTokens:string[];emergencyStop:boolean;khWalletAddress?:string|null};
export function AgentOverview(){const[agent,setAgent]=useState<Agent|null>(null);const[health,setHealth]=useState<any>(null);useEffect(()=>{void Promise.all([fetch("/backend/api/agent",{cache:"no-store"}),fetch("/backend/api/health",{cache:"no-store"})]).then(async([a,h])=>{if(a.ok)setAgent(await a.json());if(h.ok)setHealth(await h.json())})},[]);if(!agent)return <section className="agentOverview loadingState">Loading your agent…</section>;const testnet=agent.allowedChains.some(x=>x==="11155111"||x==="84532");const capabilities=[{icon:Search,title:"Autonomous research",text:"Market, news, sentiment, and onchain intelligence with x402 payment evidence.",ready:health?.llm==="configured"},{icon:Zap,title:"KeeperHub execution",text:"Simulation, guarded swaps, status reconciliation, and receipt-backed evidence.",ready:health?.execution==="configured"},{icon:BrainCircuit,title:"Long-lived memory",text:"Preferences, conversations, decisions, and context persist across every channel.",ready:health?.memory?.state==="available"},{icon:Radio,title:"Remote command",text:"Use the same agent, wallet, policy, and memory from Telegram.",ready:true}];return <div className="agentOverview"><header className="agentHero"><div><p className="eyebrow">Your autonomous capital agent</p><div className="agentState"><span className={agent.emergencyStop?"paused":"ready"}/>{agent.emergencyStop?"Paused":"Ready"}</div><h1>{agent.name}</h1><p>{agent.tradingStyle}</p><div className="heroActions"><Link href="/chat">Open Command</Link><Link href="/audit">View audit trail</Link></div></div><div className="agentIdentity"><WalletCards/><span>Agent wallet</span><b>{agent.khWalletAddress?agent.khWalletAddress.slice(0,8)+"…"+agent.khWalletAddress.slice(-6):"Provisioning"}</b><small>{testnet?"Testnet operating environment":"Mainnet operating environment"}</small></div></header><section className="readinessGrid"><Status label="KeeperHub"value={health?.execution==="configured"?"Ready":"Limited"}/><Status label="Memory"value={health?.memory?.state==="available"?"Ready":"Limited"}/><Status label="Research"value={health?.llm==="configured"?"AI enabled":"Parser only"}/><Status label="Safety"value={agent.autonomyMode==="manual"?"Approval required":"Policy guarded"}/></section><section className="overviewSection"><div className="sectionTitle"><div><p className="eyebrow">Operating policy</p><h2>Authority with hard boundaries.</h2></div><Link href="/settings">Adjust settings</Link></div><div className="policyGrid"><Policy label="Autonomy"value={agent.autonomyMode}/><Policy label="Risk"value={agent.riskProfile}/><Policy label="Daily limit"value={`${agent.dailyCapUsd} ${testnet?"test units":"USD"}`}/><Policy label="Single action"value={`${agent.perTradeCapUsd} ${testnet?"test units":"USD"}`}/><Policy label="Networks"value={testnet?"Sepolia + Base Sepolia":`${agent.allowedChains.length} mainnets`}/><Policy label="Assets"value={agent.allowedTokens.length?agent.allowedTokens.join(", "):"Policy approved"}/></div></section><section className="overviewSection"><div className="sectionTitle"><div><p className="eyebrow">Capabilities</p><h2>Research, remember, execute, report.</h2></div></div><div className="capabilityGrid">{capabilities.map(c=><article key={c.title}><c.icon/><span>{c.ready?"AVAILABLE":"LIMITED"}</span><h3>{c.title}</h3><p>{c.text}</p></article>)}</div></section><section className="agentPrompt"><ShieldCheck/><div><p className="eyebrow">Start with a goal</p><h2>Tell {agent.name} what outcome you want.</h2><p>Ask about your portfolio, request research, remember a preference, or prepare a guarded testnet trade.</p></div><Link href="/chat">Command agent</Link></section></div>}
function Status({label,value}:{label:string;value:string}){return <div><span>{label}</span><b>{value}</b></div>}function Policy({label,value}:{label:string;value:string}){return <div><span>{label}</span><b>{value}</b></div>}
