const messages:Record<string,string>={wallet_provider_conflict:"Another wallet extension signed for a different account. Disable the extra wallet extensions, reconnect one wallet, and try again.",signature_verification_failed:"Your wallet signature could not be verified. Please request a new signature and try again.",keeperhub_nonce_failed:"Wallet sign-in is temporarily unavailable. Please try again.",keeperhub_timeout:"Wallet verification timed out. Please try again.",siwe_verification_unavailable:"Wallet verification is temporarily unavailable. Please try again."};
export async function readAuthJson(response:Response):Promise<unknown>{const text=await response.text();let body:unknown=null;if(text.trim()){try{body=JSON.parse(text)}catch{throw new Error("Sign-in is temporarily unavailable. Please try again.")}}if(!response.ok){const code=body&&typeof body==="object"&&"error" in body&&typeof body.error==="string"?body.error:"";throw new Error(messages[code]??"Sign-in is temporarily unavailable. Please try again.")}if(body===null)throw new Error("Sign-in is temporarily unavailable. Please try again.");return body}
