import type { EmailSender } from "../auth/service.ts";
export function createSendGridEmailSender(deps:{readonly apiKey:string|null;readonly from:string|null;readonly fetch?:typeof globalThis.fetch}):EmailSender{return{async sendSignInLink(input){if(!deps.apiKey||!deps.from)throw new Error("Email sign in is not configured.");const response=await(deps.fetch??fetch)("https://api.sendgrid.com/v3/mail/send",{method:"POST",headers:{authorization:`Bearer ${deps.apiKey}`,"content-type":"application/json"},body:JSON.stringify({personalizations:[{to:[{email:input.email}]}],from:{email:deps.from},subject:"Sign in to Auctor",content:[{type:"text/plain",value:`Open this secure link to sign in to Auctor. It expires in 15 minutes.\n\n${input.url}`},{type:"text/html",value:`<p>Open this secure link to sign in to Auctor. It expires in 15 minutes.</p><p><a href="${input.url}">Sign in to Auctor</a></p>`}]})});if(!response.ok)throw new Error("Unable to send the sign-in email.")}}}