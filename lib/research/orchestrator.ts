import type { PaidFetchResult } from "./x402.ts";
export function isResearchRequest(text:string):boolean{return /\b(research|sentiment|market signal|market outlook|news|whale activity|onchain analysis)\b/i.test(text)}
export function researchCategory(text:string):"market"|"onchain"|"sentiment"|"news"{if(/\b(news|headline|announcement)\b/i.test(text))return"news";if(/\b(sentiment|social|mood)\b/i.test(text))return"sentiment";if(/\b(onchain|whale|wallet|flow|transaction)\b/i.test(text))return"onchain";return"market"}
export async function runPaidResearch(input:{text:string;fetchResearch(body:unknown):Promise<PaidFetchResult<unknown>>}){if(!isResearchRequest(input.text))return null;const result=await input.fetchResearch({query:input.text});return{source:"x402",paid:result.paid,priceUsd:result.priceUsd,paymentTx:result.paymentTx,refusedReason:result.refusedReason,data:result.data}}
export async function runKeyedResearch(input:{text:string;runSentiment?():Promise<unknown|null>;runNews?():Promise<unknown|null>}){if(!isResearchRequest(input.text))return null;const category=researchCategory(input.text);if(category==="sentiment"&&input.runSentiment)return input.runSentiment();if(category==="news"&&input.runNews)return input.runNews();return null}
