import{researchCategory,runPaidResearch}from"./orchestrator.ts";import type{createKeyedResearchService}from"./keyed.ts";import type{createPaidResearchService}from"./x402.ts";import type{ResearchProvider}from"./provider-request.ts";
export function createResearchRouter(input:{agentId:string;providers:readonly ResearchProvider[];keyed:ReturnType<typeof createKeyedResearchService>;paid:ReturnType<typeof createPaidResearchService>;hasAdanos:boolean;hasNews:boolean}){return{async run(text:string){const category=researchCategory(text);if(category==="sentiment"&&input.hasAdanos)return input.keyed.sentiment(text);if(category==="news"&&input.hasNews)return input.keyed.news(text);const provider=input.providers.find(item=>item.category===category)??input.providers[0];if(!provider)return null;return runPaidResearch({text,fetchResearch:body=>input.paid.fetch({agentId:input.agentId,url:provider.endpoint,provider:provider.name,body})})}}}
